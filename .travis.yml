language: python

python:
    - 3.3

env: 
    - DJANGO="Django==1.7.1"

install:
    - pip install -r requirements.txt
    - pip install coveralls

script:
    - cd behavui; coverage run --rcfile=.coveragerc manage.py test

after_success:
    - coveralls --rcfile=.coveragerc
